# EPOCH 0.8.x / 1.0.0 — Release Engineering & Stable Release

## Overview

Two-release strategy from v0.5.0 to v1.0.0:
- **v0.8.0** — Infrastructure, CI hardening, security, docs deployment
- **v1.0.0** — Test coverage, code modernization, documentation polish, stable release

## v0.8.0 — Infrastructure & Security

### package.json hardening
- Add `homepage`, `bugs`, `funding` fields
- Verify `npm pack --dry-run` includes only intended files

### CI matrix expansion
- 3 OS: ubuntu-latest, windows-latest, macos-latest
- Node 25 stable + nightly
- TS 6 (`tsc`) + TS 7 native preview (`tsgo`)
- Browser tests: Chromium, Firefox, WebKit (ubuntu only — Playwright limitation)

### Supply chain security
- Socket GitHub Action (`socket-security/socket-security-action`) on PRs
- `npm audit --omit=dev` gate in CI quality job
- npm provenance already enabled in release.yml

### Docs deployment hardening
- `robots.txt` in `docs/public/`
- VitePress built-in sitemap generation (config option)
- Lighthouse CI via `treosh/lighthouse-ci-action` in docs workflow

### Release process
- Full release checklist in CONTRIBUTING.md
- `npm pack --dry-run` + `publint` in release workflow

### Code modernization pass
- Every source file reviewed for ES2026/TS6 patterns
- Performance audit: concurrency, hot path optimization

## v1.0.0 — Hardening & Stable Release

### Test coverage
- Push to 100% across all source files
- Fill any remaining gaps

### Code modernization audit
- Line-by-line review of src/, tests/, bench/, scripts/
- Latest ES2026 idioms, TypeScript 6 strict patterns

### Documentation
- README copy-editing and badge updates
- CHANGELOG completeness verification
- TypeDoc API completeness — all symbols have examples
- Wiki final pass — all 30 pages accurate

### VitePress polish
- Theme aesthetics
- CSS performance (compositor-only animations, content-visibility)

### Release
- Version bump to 1.0.0
- Full release checklist execution
- `latest` dist-tag on npm

## Out of Scope (Manual/External)
- Screen reader testing (NVDA/JAWS/VoiceOver) — documented as manual checklist
- Community beta feedback — GitHub Discussions template
- Custom domain — optional future setup

## Existing (No Work Needed)
- npm provenance (release.yml `--provenance`)
- SECURITY.md disclosure policy
- publint in CI
- tsgo in CI
- Blog post docs/blog/0.9-beta.md
- npm audit exits 0
- Size-limit enforcement (current thresholds)
